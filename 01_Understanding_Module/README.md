# 🧮 Simple 4-bit Adder (Verilog)

이 프로젝트는 Verilog HDL(Hardware Description Language)을 이용해 만든 **4비트 덧셈기 회로** 예제입니다.  
소프트웨어처럼 실행되는 코드가 아니라, **디지털 회로를 묘사하고 시뮬레이션**하는 간단한 하드웨어 설계 프로젝트입니다.

---

## 📘 개념

Verilog는 **HDL (Hardware Description Language)**, 즉 하드웨어를 "코드로 설명"하는 언어입니다.  
일반적인 프로그래밍 언어(C, Python 등)가 순차적 명령을 실행하는 것과 달리,  
Verilog는 **전기적 연결과 병렬 동작**을 표현합니다.

예를 들어, 다음 한 줄

```verilog
assign sum = a + b;
```
은 "`a`와 `b`를 더하는 연산을 수행하라"가 아니라
**"회로상에서 `sum`이 항상 `a+b`의 결과를 나타내도록 연결한다"**는 의미입니다.

## 📘 파일 구성

```bash
verilog_test/
├── adder.v        # 덧셈기 회로 정의
└── tb_adder.v     # 테스트벤치 (시뮬레이션용)
```
`adder.v`

```verilog
module adder (
    input wire [3:0] a, b,
    output wire [4:0] sum
);
    assign sum = a + b;
endmodule
```
4비트 입력 두 개(`a`, `b`)를 받아, 5비트 덧셈 결과 (`sum`)를 출력하는 단순 조합논리 회로입니다.
`tb_adder.v`

```verilog
`timescale 1ns/1ps

module tb_adder;
    reg [3:0] a, b;
    wire [4:0] sum;

    adder uut (.a(a), .b(b), .sum(sum));

    initial begin
        $dumpfile("dump.vcd");
        $dumpvars(0, tb_adder);

        a = 4'b0011; b = 4'b0101; #10;
        a = 4'b1111; b = 4'b0001; #10;
        a = 4'b0110; b = 4'b0110; #10;

        $finish;
    end

    initial begin
        $monitor("time=%0t, a=%b, b=%b, sum=%b", $time, a, b, sum);
    end
endmodule

```
이 파일은 실제 회로 (`adder`)를 불러와서 입력 신호를 바꾸고, 출력 결과를 모니터링하는 시뮬레이션 스크립트입니다.
시뮬레이션 결과는 콘솔 출력과 함께 **파형(waveform)** 파일 (`dump.vcd`)로 저장됩니다.

## 📘 실행 방법

### 필수 도구 설치 (Ubuntu/Linux)
```bash
sudo apt update
sudo apt install iverilog gtkwave -y
```

### 프로젝트 폴더 준비
```bash
git clone https://github.com/<your-username>/verilog-adder.git
cd verilog-adder
```

### 컴파일
```bash
iverilog -o testbench.out tb_adder.v adder.v
```

### 시뮬레이션 실행
```bash
vvp testbench.out
```

터미널에서 아래와 같은 출력 확인 가능 :
```
time=0, a=0011, b=0101, sum=01000
time=10, a=1111, b=0001, sum=10000
time=20, a=0110, b=0110, sum=01100
```

### 파형 보기
```bash
gtkwave dump.vcd
```
GTKWave창에서 `a`, `b`, `sum` 신호를 선택하면, 시간에 따라 값이 변하는 것을 시각적으로 확인할 수 있습니다.

## 배운 개념 정리
| 개념 | 설명 |
|-----|-----|
| module | 회로 블록 정의 |
| assign | 신호 간의 연결 관계 표현 |
| input / output | 회로의 입출력 포트 |
| reg / wire | 시뮬레이션 변수 및 연결선 |
| testbench | 회로 테스트용 환경 (입력/출력 관찰) |
| $dumpfile / $dumpvars | 파형 파일 생성 |
| #10 | 시뮬레이션 시간 지연 (10ms) |
| $monitor | 값 변화를 콘솔에 출력 |

## 확장 아이디어
- 덧셈기(Adder)에 **감산 기능** 추가하기
- `always @(posedge clk)` 구문을 사용해 **클럭 기반 누산기(accumulator)** 만들기
- **Verilator**로 시뮬레이션 속도를 구하기
- FPGA보드 (Vivado, Quartus등)에서 실제로 동작시키기